checkpoint_path: "/home/eggers/data/apexdag_results/jetbrains_dfg_100k_new_labeled/execution_graphs/"
encoded_checkpoint_path: "data/raw/pytorch-embedded-finetune-new"
pretrained_model_path: "checkpoints/checkpoints-swapped/model_epoch_pretrained_10.pt"
hidden_dim: 128
num_heads: 4
node_classes: 9 # reversed
edge_classes: 8
batch_size: 32
learning_rate: 0.001
num_epochs: 20
train_split: 0.6
test_split: 0.1
val_split: 0.2
patience: 5

# hyperparameters for encoder
min_nodes: 3
min_edges: 2

load_encoded_old_if_exist: True # if the graphs are encoded do not encode them once again.
weight_decay: 0.001
dim_embed: 300
residual: True
number_gat_blocks: 3
dropout: 0.2
device: "cpu"